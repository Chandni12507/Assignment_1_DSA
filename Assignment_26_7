#include<stdio.h>
#include<stdlib.h>

int main()
{
    int n , m=0;
    n= rand()%5289 + 950383082;
    printf("%d", &n);
    while(n>0)
    {
        a = n%10;
        n = n/10;
        if(a>m)
        {
            max = a;
        }
        a=m;
    }
    printf("Highest value: %d",&max);
}
